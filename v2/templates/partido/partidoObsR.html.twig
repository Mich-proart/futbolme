{% if partido.observaciones %}
    {% if cadena.0|length > 10 %}
        <div class="row">
            <div class="col-12 text-right" style="font-size:13px; margin-top: 10px; margin-bottom: 10px;">
                <i>{{ cadena.0 }}</i>
            </div>
        </div>
    {% endif %}
{% endif %}